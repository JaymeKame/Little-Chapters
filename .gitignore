node_modules/
.next/
out/
*.log
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo

# Secrets — never committed
.env
.env.local
.env*.local

# Per-user local Claude Code settings (hooks, permissions) — never committed
.claude/settings.local.json

# MDD decoding service
mdd/.venv/
__pycache__/
.vercel
